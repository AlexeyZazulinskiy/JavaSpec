import java.util.List;

public class model {
    public static void getRandom(){ // возвращает рандомную ирушку с учетом шанса
        
    }

    public static Toy[] getWinList(){  // возвращает список разыгрываемых игрушек
        Toy[] toyList = new Toy[]{};
        return toyList;
    }
}
